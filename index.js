/**
 * Color Average
 * @param  {...any} params - hex, rsl, rgb
 */
function colorAvg(...params) {
  const inputs = params;
  // pass into validation function here

  // then pass into average function here
}

colorAvg.prototype = {
  // util functions here
};

export default colorAvg;
